@PACKAGE_INIT@

# rednoise is a self-contained static/shared library (glm is compiled in, so
# downstream needs nothing extra). Just import the targets.
include("${CMAKE_CURRENT_LIST_DIR}/rednoiseTargets.cmake")

check_required_components(rednoise)
